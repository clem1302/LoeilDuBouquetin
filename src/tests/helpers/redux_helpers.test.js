import {mapDispatchToProps, mapStateToProps} from "../../helpers/redux_helpers";

it("should success", () => {
  mapDispatchToProps({});
});

it("should success", () => {
  mapStateToProps({a:1});
});
